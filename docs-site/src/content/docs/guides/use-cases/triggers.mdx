---
title: Fire a task from an external event
description: "A trigger turns an outside event into platform work: an authenticated HTTP POST — or a file landing in S3, via EventBridge and SQS — enqueues a task such as a SQL statement or a file ingest. Each trigger belongs to one workspace, carries its own authentication, and records every firing so a failure shows the reason verbatim rather than an opaque error."
---

{/* GENERATED by docs-site/scripts/gen-usecases-reference.mjs.
    Steps and screenshots come from the capture run — edit the use-case in
    frontend/e2e/usecases/definitions/. Prose goes in
    src/content/use-case-intros/<id>.intro.md, and the CLI/MCP/API routes in
    src/data/use-case-surfaces.json. */}

A trigger turns an outside event into platform work: an authenticated HTTP POST — or a file landing in S3, via EventBridge and SQS — enqueues a task such as a SQL statement or a file ingest. Each trigger belongs to one workspace, carries its own authentication, and records every firing so a failure shows the reason verbatim rather than an opaque error.

The screenshots below use **service token** authentication, where callers present an
OAuth2 client-credentials token and the calling service principal must be bound to
this workspace. Two other modes exist:

- **Webhook secret** — Standard Webhooks HMAC signing, for senders that cannot
  fetch a token. The Connect dialog then also reveals and rotates a `whsec_`
  secret, which is why that mode is not pictured here.
- **No authentication** — available only where the platform sets
  `TRIGGERS_ALLOW_UNAUTHENTICATED`, which is off by default.

:::note
"Test fire" sends the event through the real path: a task is enqueued and the
statement runs as the trigger's owner. It works while the trigger is disabled, and
it appears in Monitoring like any other firing — so a test is visible in the
history rather than hidden from it.
:::

**Who does this:** workspace-admin

## Steps

1. Open the Triggers page for the workspace
2. Describe the trigger: a name, an HTTP source, and how callers authenticate

   ![Describe the trigger: a name, an HTTP source, and how callers authenticate](../../../../assets/screenshots/triggers-create.png)

3. Create it, and see it listed for the workspace

   ![Create it, and see it listed for the workspace](../../../../assets/screenshots/triggers-list.png)

4. Read the connection details a caller needs: the events URL and a worked example

   ![Read the connection details a caller needs: the events URL and a worked example](../../../../assets/screenshots/triggers-connect.png)

5. Send a test event, so the trigger fires through the real dispatch path
6. Check the Monitoring tab: how many actions each trigger dispatched

   ![Check the Monitoring tab: how many actions each trigger dispatched](../../../../assets/screenshots/triggers-monitoring.png)

7. Read the recent runs for one trigger — each firing, its outcome, and any reason

   ![Read the recent runs for one trigger — each firing, its outcome, and any reason](../../../../assets/screenshots/triggers-runs.png)


## Other ways to reach this

The steps above were executed against a live platform and photographed. The
commands below were not: they are checked to exist when the documentation is
built, which is a weaker guarantee. Follow the links for arguments and options.

### Command line

- [`chameleon trigger create`](/reference/cli/)
- [`chameleon trigger list`](/reference/cli/)
- [`chameleon trigger connection`](/reference/cli/)
- [`chameleon trigger test`](/reference/cli/)
- [`chameleon trigger runs`](/reference/cli/)

### MCP tools

No trigger tool is exposed to MCP.

### HTTP API (for developers)

- [`POST /api/platform/v1/triggers`](/reference/api/operations/create_trigger_api_platform_v1_triggers_post/)
- [`POST /api/platform/v1/triggers/{trigger_id}/test`](/reference/api/operations/test_fire_trigger_api_platform_v1_triggers__trigger_id__test_post/)
- [`GET /api/platform/v1/triggers/runs`](/reference/api/operations/get_trigger_runs_api_platform_v1_triggers_runs_get/)

These are the ws-admin endpoints, which take an `X-Workspace` header. They are not the machine-facing `POST /triggers/{id}/events` endpoint a caller fires the trigger with — the portal's Connect dialog generates a worked example of that one.
