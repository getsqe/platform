---
title: Browse the data catalog
description: "The catalog is the map of what exists. A catalog holds namespaces, a namespace holds tables, and each table exposes the Iceberg schema and metadata that every engine on the platform reads — one definition rather than a copy per tool."
---

{/* GENERATED by docs-site/scripts/gen-usecases-reference.mjs.
    Steps and screenshots come from the capture run — edit the use-case in
    frontend/e2e/usecases/definitions/. Prose goes in
    src/content/use-case-intros/<id>.intro.md, and the CLI/MCP/API routes in
    src/data/use-case-surfaces.json. */}

The catalog is the map of what exists. A catalog holds namespaces, a namespace holds tables, and each table exposes the Iceberg schema and metadata that every engine on the platform reads — one definition rather than a copy per tool.

A catalog belongs to exactly one workspace, so what you see here is your
workspace's data — not the platform's. The path narrows in the same order
everywhere: catalog, then namespace (a schema), then table.

The table page reads Iceberg metadata rather than the data itself, which is why
it is fast and why it can tell you things a `SELECT` cannot: the current schema,
the partitioning, and the snapshot history. Snapshots are a record of how the
table changed, not a backup — see
[storage and tables](/docs/concepts/storage-and-tables/).

:::caution[An empty namespace is not proof it is empty]
Access is decided per user, and a denied read is not an error — a namespace whose
tables you have no grant on renders as "no tables", with nothing to indicate why.
If you expect something to be there, treat it as an
[access](/docs/concepts/access-control/) question first, not a missing-data one.
:::

**Who does this:** analyst

## Steps

1. Open the catalog explorer

   ![Open the catalog explorer](../../../../assets/screenshots/browse-catalog-overview.png)

2. Open the catalog to see its properties and contents

   ![Open the catalog to see its properties and contents](../../../../assets/screenshots/browse-catalog-catalog.png)

3. Open a namespace to list its tables

   ![Open a namespace to list its tables](../../../../assets/screenshots/browse-catalog-namespace.png)

4. Open a table to read its Iceberg metadata

   ![Open a table to read its Iceberg metadata](../../../../assets/screenshots/browse-catalog-table.png)


## Other ways to reach this

The steps above were executed against a live platform and photographed. The
commands below were not: they are checked to exist when the documentation is
built, which is a weaker guarantee. Follow the links for arguments and options.

### Command line

- [`chameleon data catalog`](/docs/reference/cli/)
- [`chameleon data namespace`](/docs/reference/cli/)
- [`chameleon data table`](/docs/reference/cli/)

### MCP tools

- [`list_workspace_catalogs`](/docs/reference/mcp-tools/)
- [`list_namespaces`](/docs/reference/mcp-tools/)
- [`list_tables`](/docs/reference/mcp-tools/)
- [`describe_table`](/docs/reference/mcp-tools/)

### HTTP API (for developers)

- [`GET /api/platform/v1/catalogs`](/docs/reference/api/operations/list_catalogs_api_platform_v1_catalogs_get/)
- [`GET /api/platform/v1/catalogs/{catalog_name}/namespaces/{namespace_path}/tables`](/docs/reference/api/operations/list_tables_api_platform_v1_catalogs__catalog_name__namespaces__namespace_path__tables_get/)
