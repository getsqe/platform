---
title: "chameleon_ontology_attribute"
---

# chameleon_ontology_attribute (Resource)

A column-like field on a `chameleon_ontology_entity_type`. `data_type` is one of `string` / `number` / `integer` / `boolean` / `date` / `datetime` / `ref`. Names are snake_case.

## Example Usage

```terraform
resource "chameleon_ontology_attribute" "customer_email" {
  entity_type_id = chameleon_ontology_entity_type.customer.id
  name           = "email"
  display_name   = "Email"
  data_type      = "string" # string | number | integer | boolean | date | datetime | ref
  is_required    = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `data_type` (String) string | number | integer | boolean | date | datetime | ref
- `display_name` (String) Human-readable label.
- `entity_type_id` (String) Parent entity type.
- `name` (String) snake_case identifier.

### Optional

- `description` (String) Free-text description.
- `iri` (String) Optional fully-qualified IRI.
- `is_identifier` (Boolean) Marks this attribute as the entity's primary identifier.
- `is_required` (Boolean) Non-nullable when true.

### Read-Only

- `created_at` (String) RFC3339 creation timestamp.
- `id` (String) BFF-assigned UUID.
- `lock_version` (Number) Optimistic-concurrency token threaded through PATCH calls automatically.
- `version_id` (String) Ontology version (inherited from the entity type).
