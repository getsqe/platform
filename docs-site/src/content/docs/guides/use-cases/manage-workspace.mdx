---
title: Manage workspaces
description: "A workspace is the unit of tenancy: it owns a catalog, its own storage buckets, and the Keycloak groups whose members administer or use it. One catalog belongs to exactly one workspace, which is what keeps tenants separated in the data plane."
---

{/* GENERATED by docs-site/scripts/gen-usecases-reference.mjs.
    Steps and screenshots come from the capture run — edit the use-case in
    frontend/e2e/usecases/definitions/. Prose goes in
    src/content/use-case-intros/<id>.intro.md, and the CLI/MCP/API routes in
    src/data/use-case-surfaces.json. */}

A workspace is the unit of tenancy: it owns a catalog, its own storage buckets, and the Keycloak groups whose members administer or use it. One catalog belongs to exactly one workspace, which is what keeps tenants separated in the data plane.

Creating a workspace is not just a row in a list. The slug you choose becomes the
name of everything the workspace gets: its own catalog, its storage (a warehouse
bucket and a staging bucket), and two groups — `ws-<slug>-admins` and
`ws-<slug>-members` — which are what membership actually means.

That is why the slug is worth a moment's thought: it is visible in table names,
in storage paths and in group names afterwards.

The split between the two groups is the one to understand. A **member** can use
the workspace. An **admin** can manage its members and grant access on its own
catalog — and only on its own catalog. What that does and does not isolate is in
[tenancy and isolation](/docs/concepts/multi-tenancy/).

**Who does this:** admin

## Steps

1. Open the workspace administration page

   ![Open the workspace administration page](../../../../assets/screenshots/manage-workspace-list.png)


## Other ways to reach this

The steps above were executed against a live platform and photographed. The
commands below were not: they are checked to exist when the documentation is
built, which is a weaker guarantee. Follow the links for arguments and options.

### Command line

- [`chameleon workspace create`](/docs/reference/cli/)
- [`chameleon workspace list`](/docs/reference/cli/)
- [`chameleon workspace show`](/docs/reference/cli/)

### MCP tools

- [`create_workspace`](/docs/reference/mcp-tools/)
- [`list_workspaces`](/docs/reference/mcp-tools/)
- [`show_workspace`](/docs/reference/mcp-tools/)

### HTTP API (for developers)

- [`GET /api/platform/v1/workspaces`](/docs/reference/api/operations/list_workspaces_api_platform_v1_workspaces_get/)
- [`PUT /api/platform/v1/workspaces/{slug}`](/docs/reference/api/operations/upsert_workspace_api_platform_v1_workspaces__slug__put/)

Creation is a PUT on the slug, and it is idempotent — the same request twice yields one workspace.
