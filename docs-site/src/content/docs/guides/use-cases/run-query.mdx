---
title: Run a query
description: "The query editor runs SQL against the lakehouse through the platform, so every statement is authorized as the signed-in user rather than a shared service identity. The same query is therefore subject to the same grants whichever engine executes it."
---

{/* GENERATED by docs-site/scripts/gen-usecases-reference.mjs.
    Steps and screenshots come from the capture run — edit the use-case in
    frontend/e2e/usecases/definitions/. Prose goes in
    src/content/use-case-intros/<id>.intro.md, and the CLI/MCP/API routes in
    src/data/use-case-surfaces.json. */}

The query editor runs SQL against the lakehouse through the platform, so every statement is authorized as the signed-in user rather than a shared service identity. The same query is therefore subject to the same grants whichever engine executes it.

The statement runs as **you**. There is no shared service identity behind the
editor, so a query returns what your own grants allow and the same SQL can
legitimately return different rows for two people.

Name tables in full — `catalog.namespace.table` — whenever you are not sure what
the session default is. A workspace's queries default to that workspace's own
catalog, so an unqualified name is resolved there, and a query that "works for
them but not for me" is usually a different default rather than a different
permission.

The engine is shown next to the editor. Which engines exist is a platform
decision; choosing between the ones that are enabled is yours — the control is a
plain label when only one is available and a picker when more than one is, offering
SQE (Flight), SQE (Trino) and Spark. The authorization outcome is meant to be the
same whichever you pick. Where the engine changes *where* the decision is made —
and why that matters when debugging — is in [engines](/docs/concepts/engines/).

:::note
A failed query reports the engine's own error. That is deliberate: a truncated or
prettified message is the difference between "table not found" (wrong name) and
"access denied" (right name, no grant), and those need different fixes.
:::

**Who does this:** analyst

## Steps

1. Open the query editor

   ![Open the query editor](../../../../assets/screenshots/run-query-editor.png)

2. Run a SELECT and read the results

   ![Run a SELECT and read the results](../../../../assets/screenshots/run-query-results.png)


## Other ways to reach this

The steps above were executed against a live platform and photographed. The
commands below were not: they are checked to exist when the documentation is
built, which is a weaker guarantee. Follow the links for arguments and options.

### Command line

- [`chameleon data query`](/docs/reference/cli/)

### MCP tools

- [`query_data`](/docs/reference/mcp-tools/)

### HTTP API (for developers)

- [`POST /api/platform/v1/query`](/docs/reference/api/operations/execute_query_api_platform_v1_query_post/)
- [`POST /api/platform/v1/query/explain`](/docs/reference/api/operations/explain_query_api_platform_v1_query_explain_post/)

The query runs as the calling identity, so results are filtered by that identity's grants — the same rule the portal follows.
